# -*- mode: python ; coding: utf-8 -*-


block_cipher = None


a = Analysis(['hidden', '-import=tensorflow--hidden-import=tensorflow.lite.python.lite', '-F', 'run.py'],
             pathex=['C:\\Context_Classify\\venv\\Lib\\tensorflow', 'C:\\Context_Classify'],
             binaries=[],
             datas=[],
             hiddenimports=[],
             hookspath=[],
             runtime_hooks=[],
             excludes=[],
             win_no_prefer_redirects=False,
             win_private_assemblies=False,
             cipher=block_cipher,
             noarchive=False)
pyz = PYZ(a.pure, a.zipped_data,
             cipher=block_cipher)
exe = EXE(pyz,
          a.scripts,
          [],
          exclude_binaries=True,
          name='hidden',
          debug=False,
          bootloader_ignore_signals=False,
          strip=False,
          upx=False,
          console=True )
coll = COLLECT(exe,
               a.binaries,
               a.zipfiles,
               a.datas,
               strip=False,
               upx=False,
               upx_exclude=[],
               name='hidden')
